import { FC } from "react";

import { Container } from "@/shared/ui";

const Header: FC = () => {
  return (
    <header className="">
      <Container></Container>
    </header>
  );
};

export default Header;
